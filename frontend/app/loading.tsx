import { RouteLoader } from "@/components/RouteLoader";

export default function Loading() {
  return (
    <RouteLoader
      title="Loading FoodHub"
      message="Your next page is on the way."
    />
  );
}
